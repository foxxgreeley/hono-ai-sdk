{
  "name": "hono-ai-sdk",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "generate-types": "wrangler types",
    "type-check": "generate-types && tsc"
  },
  "dependencies": {
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.4.0"
  }
}
